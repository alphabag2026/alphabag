server {
    listen 80;
    server_name alphabag.net www.alphabag.net;

    # Let's Encrypt 인증용
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # HTTP → HTTPS 리다이렉트 (SSL 설정 후 활성화)
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # 임시: HTTP로 직접 서비스
    location / {
        proxy_pass http://app:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# HTTPS 설정 (SSL 인증서 발급 후 활성화)
# server {
#     listen 443 ssl;
#     server_name alphabag.net www.alphabag.net;
#
#     ssl_certificate /etc/letsencrypt/live/alphabag.net/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/alphabag.net/privkey.pem;
#
#     location / {
#         proxy_pass http://app:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_read_timeout 86400;
#     }
# }
